# Text Passages Part

NDP-specific. This is a collection of text passages.

🔑 `it.vedph.ndp.text-passages`

- `passages`* (`TextPassage[]`):
  - `tag` (`string`, 📚 `text-passage-tags`)
  - `features` (`string[]`, 📚 `text-passage-features`, hierarchical)
  - `location`\* (e.g. `@dc:If. I 3`)
  - `text` (`string`): text passage.
  - `note` (`string`)
